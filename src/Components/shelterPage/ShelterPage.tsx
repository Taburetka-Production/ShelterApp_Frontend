import { Shelter, Address } from "@/redux/types";
import { useState } from "react";

export const ShelterPage = ({
  shelter,
  isEditMode,
}: {
  shelter?: Shelter;
  isEditMode?: boolean;
}) => {
  return {};
};
